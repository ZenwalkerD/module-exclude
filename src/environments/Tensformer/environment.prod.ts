import { tensformerBase } from './environment-base';

export const environment = {
  production: true,
  routes: tensformerBase.routes,
  modules: tensformerBase.modules,
  buildText: 'prod build tensformer'
};
